{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimum core permissions for the main Markora window. Native file access is exposed only through validated custom commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
