{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Least-privilege desktop capability for the main Markora window. Native document access remains behind validated application commands.",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": ["core:event:default", "desktop-workspace-commands"]
}
