{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile-document-capability",
  "description": "Android and iOS permissions for user-selected Markdown, backup, and export files. Dialog-selected paths are added to the runtime filesystem scope and persisted by the persisted-scope plugin.",
  "windows": ["main"],
  "platforms": ["iOS", "android"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:allow-stat",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "opener:allow-default-urls"
  ]
}
